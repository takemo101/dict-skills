# Issue #302 実装計画

## 概要

SKILL.mdから存在しないドキュメントファイルへのリンクを削除する。対象となるリンクは以下の通り：

1. `[CLI仕様書](../docs/link-crawler/cli-spec.md)` - 2箇所
2. `[設計書](../docs/link-crawler/design.md)` - 1箇所

## 影響範囲

- **対象ファイル**: `link-crawler/SKILL.md`
- **変更箇所**:
  - 行51付近: オプション一覧セクション末尾のCLI仕様書リンク
  - 行69付近: 出力ファイルセクション末尾のCLI仕様書リンク
  - 行79付近: 参考リンクセクション全体

## 実装ステップ

1. SKILL.mdを読み込み、該当するリンクを特定
2. 以下の修正を実施：
   - 2箇所の「詳細な仕様は [CLI仕様書]...」という文言を削除
   - 「参考リンク」セクションを削除（存在しないドキュメントへのリンクのみ含むため）
3. 変更後のファイルを保存
4. 変更内容を確認

## テスト方針

- ビジュアルレビュー: 削除後のドキュメントが自然に読めることを確認
- リンクチェック: 残存するリンクが有効か確認（存在しない場合は他にも修正が必要）

## リスクと対策

| リスク | 対策 |
|--------|------|
| ドキュメントの文脈が不自然になる | 削除するのはリンク部分のみで、前後の文脈は維持 |
| 他にも存在しないリンクがある | 全文検索で `](` パターンを確認し、他のリンクも検証 |

## 完了条件

- [ ] SKILL.mdから存在しないファイルへのリンクが削除されている
- [ ] ドキュメントの可読性が維持されている
- [ ] 変更がコミットされている
