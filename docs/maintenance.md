# Maintenance Guide

このドキュメントでは、dict-skills プロジェクトの定期的なメンテナンス作業について説明します。

## 開発アーティファクトのクリーンアップ

### 自動スクリプトの使用

プロジェクトには開発アーティファクトをクリーンアップする自動化スクリプトが含まれています。

```bash
# ドライラン（何が削除されるか確認）
./scripts/cleanup-dev-artifacts.sh

# 詳細な出力を見る
./scripts/cleanup-dev-artifacts.sh --verbose

# 実際に削除を実行（30日以上前のファイル）
./scripts/cleanup-dev-artifacts.sh --force

# 保持期間を7日に設定
./scripts/cleanup-dev-artifacts.sh --days 7 --force
```

### スクリプトが実行すること

1. **`.improve-logs/` のクリーンアップ**
   - 指定期間（デフォルト30日）より古いログファイルを削除
   - 削除対象のファイル数とサイズを表示

2. **`.worktrees/` のチェック**
   - 古いワークツリーの検出
   - マージ済みブランチのワークツリーを特定
   - 手動での削除を提案

### 手動クリーンアップ

自動スクリプトを使用せずに手動でクリーンアップする場合：

#### .improve-logs/ のクリーンアップ

```bash
# 30日以上前のログファイルを削除
find .improve-logs/ -type f -mtime +30 -delete

# 削除前に確認
find .improve-logs/ -type f -mtime +30 -ls
```

#### .worktrees/ のクリーンアップ

```bash
# アクティブなワークツリーを確認
git worktree list

# 不要になったワークツリーの削除
git worktree remove .worktrees/issue-XXX-description

# 関連するブランチも削除（マージ済みの場合）
git branch -d feature/issue-XXX-description

# 古いワークツリーのパスを整理
git worktree prune
```

### マージ済みブランチの確認

```bash
# main にマージ済みのブランチを一覧表示
git branch --merged main | grep -v main

# リモートにもローカルにもマージ済みのブランチを削除
git branch --merged main | grep -v main | xargs git branch -d
```

## 定期的なメンテナンス推奨事項

### 週次

- [ ] アクティブなワークツリーを確認し、完了したものを削除
- [ ] `.improve-logs/` のサイズを確認

### 月次

- [ ] 30日以上前の `.improve-logs/` を削除
- [ ] マージ済みブランチの整理
- [ ] ディスク使用量の確認

## トラブルシューティング

### ワークツリーの削除に失敗する

```bash
# ワークツリーの強制削除
git worktree remove --force .worktrees/issue-XXX-description

# ワークツリーが削除できない場合は手動で
rm -rf .worktrees/issue-XXX-description
git worktree prune
```

### ディスク容量の確認

```bash
# .improve-logs/ のサイズ
du -sh .improve-logs/

# .worktrees/ のサイズ
du -sh .worktrees/

# プロジェクト全体のサイズ
du -sh .
```

### 削除してはいけないもの

以下のディレクトリ・ファイルは削除しないでください：

- `.worktrees/.context/` - ワークツリー間で共有されるコンテキスト情報
- `.worktrees/.status/` - ワークツリーのステータス管理
- アクティブなワークツリー（`git worktree list` で表示されるもの）

## 自動化の推奨

定期的なクリーンアップを自動化する場合：

### Cron ジョブ（Unix/Linux/macOS）

```bash
# crontab を編集
crontab -e

# 毎週日曜日の午前3時に実行
0 3 * * 0 cd /path/to/dict-skills && ./scripts/cleanup-dev-artifacts.sh --force --days 30 >> /tmp/dict-skills-cleanup.log 2>&1
```

### Git フックの利用

```bash
# post-merge フックでクリーンアップを提案
cat > .git/hooks/post-merge << 'EOF'
#!/bin/bash
echo "💡 Tip: Run './scripts/cleanup-dev-artifacts.sh' to clean up old development artifacts"
EOF

chmod +x .git/hooks/post-merge
```

## 参考リンク

- [Git Worktree Documentation](https://git-scm.com/docs/git-worktree)
- [プロジェクトの .gitignore](../.gitignore)
- [クリーンアップスクリプト](../scripts/cleanup-dev-artifacts.sh)
