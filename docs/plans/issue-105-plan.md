# GitHub Issue #105 実装計画

## Issue概要

README.md にインストール手順と使用例が `TODO` のまま未記載。SKILL.md には詳細なドキュメントがあるが、プロジェクトルートの README が未完成のため、初見のユーザーが使い方を把握しにくい。

## 影響範囲

- **対象ファイル**: `README.md`（プロジェクトルート）
- **参照ファイル**: `link-crawler/SKILL.md`（詳細ドキュメント）
- **依存関係**: なし（ドキュメントのみの変更）

## 実装ステップ

### Step 1: README.md の内容を更新

以下の内容で `README.md` を完全に書き換える：

1. **タイトルと説明**（既存）
   - `# dict-skills`
   - 対象ページからリンクを辿って情報を収集する pi スキル

2. **概要**（既存、一部修正）
   - 指定されたWebページを起点として、リンクを再帰的に辿りながら情報を収集

3. **機能**（拡張）
   - 指定URLからのリンク探索
   - 深さ制限付きクローリング
   - 収集した情報の構造化
   - **追加**: 差分クロール（変更のみ更新）
   - **追加**: AI向け結合ファイル出力

4. **インストール**（新規）
   - `cd link-crawler` で移動
   - `bun install` で依存関係インストール
   - **前提条件**: Bun 1.3.x 以上、playwright-cli

5. **使用方法**（新規）
   - 基本的なクロールの例
   - 出力先を指定する例
   - AIコンテキスト用の例（結合ファイルのみ）
   - SKILL.md へのリンク

6. **ライセンス**（既存）
   - MIT

### Step 2: 検証

- `cat README.md` で内容を確認
- `grep -n "TODO" README.md` でTODOが残っていないことを確認（期待: 0件）

## テスト方針

- ドキュメント変更のため、コードのテストは不要
- 変更後の手動検証:
  1. README.md の構文が正しいか
  2. すべてのセクションが含まれているか
  3. TODO が削除されているか
  4. リンクが正しく機能するか

## リスクと対策

| リスク | 対策 |
|-------|------|
| ドキュメントが不完全 | SKILL.md を参照して正確な情報を含める |
| インストール手順が環境依存 | 前提条件を明確に記載 |
| 使用方法の例が古い | 実際の package.json のスクリプトに基づく |

## 完了条件

- [x] README.md のインストール手順を記載
- [x] README.md の使用例を記載
- [x] TODO コメントを削除
- [x] SKILL.md へのリンクを追加
